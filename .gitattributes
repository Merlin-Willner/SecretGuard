# Normalize line endings: text files use LF.
* text=auto eol=lf

# Shell scripts and hooks must stay LF for shebangs.
*.sh text eol=lf
.githooks/* text eol=lf

# Windows batch files keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# Common binary formats.
*.png -text
*.jpg -text
*.jpeg -text
*.gif -text
*.pdf -text
